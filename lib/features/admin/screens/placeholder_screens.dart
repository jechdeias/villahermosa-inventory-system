import 'package:flutter/material.dart';

/// Placeholder screens for admin navigation
/// These will be replaced with actual implementations

class ProductsPlaceholderScreen extends StatelessWidget {
  const ProductsPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon: Products',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}

class CustomersPlaceholderScreen extends StatelessWidget {
  const CustomersPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon: Customers',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}

class OrdersPlaceholderScreen extends StatelessWidget {
  const OrdersPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon: Orders',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}

class StockPlaceholderScreen extends StatelessWidget {
  const StockPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon: Stock Management',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}

class DeliveriesPlaceholderScreen extends StatelessWidget {
  const DeliveriesPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon: Deliveries',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}

class ReportsPlaceholderScreen extends StatelessWidget {
  const ReportsPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon: Reports',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}

class SettingsPlaceholderScreen extends StatelessWidget {
  const SettingsPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon: Settings',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
